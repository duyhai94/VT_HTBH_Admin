export interface ListEmployeeModel{
          is: string;
          sellerCode: string,
          code: string,
          created: string,
          effectiveDate: string,
          expiredDate: string,
          feeAmount: string,
          insuredFullName:string
}